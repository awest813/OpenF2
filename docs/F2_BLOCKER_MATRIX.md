# Fallout 2 Critical Path Blocker Matrix

Use this matrix to track only blockers that impact campaign completion or fidelity on the critical path.

Severity guidance:
- `CRITICAL`: blocks or corrupts campaign progression
- `HIGH`: major fidelity issue with high risk of progression failure
- `MEDIUM`: noticeable fidelity gap with workaround

Status guidance:
- `OPEN`
- `IN_PROGRESS`
- `FIXED_PENDING_TEST`
- `CLOSED`

---

| ID | Severity | Region | Map/Script | Procedure/Opcode | Symptom | Repro Steps | Expected Behavior | Automated Test Coverage | Owner | Status | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| BLK-001 | HIGH | Global/VM | `src/scripting.ts` | `tile_num_in_direction` + scripting type paths | TypeScript compile failed due duplicate function implementation and invalid type usages in scripting bridge methods. | `npx tsc --noEmit` produced TS2393/TS2345/TS2339 errors. | VM scripting module compiles cleanly with one canonical `tile_num_in_direction` implementation and type-safe logging/object narrowing. | `npx tsc --noEmit`, `src/phase24.test.ts` | @engine | CLOSED | Fixed by removing duplicate method, correcting log/warn argument typing, and narrowing `source_obj` union. |
| BLK-002 | HIGH | Global/VM | `metarule3` runtime | `metarule3_103` | `METARULE3_CRITTER_IN_COMBAT` answered from global flag only, not per-critter participation. | Call `metarule3(103, critter, ...)` during combat with critter absent from combat roster. | Return 1 only for critters in active combatant list (fallback to global flag only when roster unavailable). | `src/phase22.test.ts` (new roster and fallback assertions) | @engine | CLOSED | Checklist entry promoted to implemented after regression coverage. |
| BLK-003 | HIGH | Interaction/UI2 | barter session loop | `BarterPanel._tryOffer` | Accepted offers cleared barter tables without committing exchanged items into working inventories, causing item loss in repeated barter rounds. | Move one item from each side to tables and click OFFER in UI2 barter panel. | Accepted offers transfer merchant-table items into player inventory and player-table items into merchant inventory; tables then clear. | `src/ui2/ui2.test.ts`, `src/ui2/panelParity.test.ts` | @engine | CLOSED | Added inventory merge-on-accept behavior + regression coverage for accepted/refused flows. |
| BLK-004 | HIGH | World Map | world-map session restore | `Worldmap.init` starting position | World-map session always initialized at Arroyo hotspot, ignoring saved world travel position and risking travel-state discontinuity after load. | Provide saved world position, initialize world map, observe cursor starts at default area instead of saved point. | World-map initialization restores saved position (clamped to bounds) and keeps global worldPosition synchronized during movement. | `src/phase28.test.ts`, `src/worldmapEncounter.test.ts` | @engine | CLOSED | Added normalization helper + runtime sync to global worldPosition for save continuity. |
| BLK-005 | HIGH | Save/Load | runtime persistence integration | `saveload` world-position/script globals path | Runtime save/load resilience suite had no integration assertion proving `save()` persisted worldPosition and `load()` restored worldPosition + script globals together. | Save slot with non-default world position and marker GVAR; mutate runtime values; load slot. | Loaded state restores saved worldPosition and scripted global variable markers. | `src/saveload.test.ts` | @engine | CLOSED | Added in-memory backend integration regressions for worldPosition persistence + load restoration. |
| BLK-006 | HIGH | Combat | AP accounting | `ActionPoints.subtractCombatAP/subtractMoveAP` | Negative AP spend inputs could increase AP due subtraction of negative values, allowing AP-gain exploit in malformed call paths. | Call `subtractCombatAP(-n)` or `subtractMoveAP(-n)` on live ActionPoints object. | Non-positive AP spend values are treated as no-op and cannot increase AP. | `src/combat.integration.test.ts` | @engine | CLOSED | Added guards for `value <= 0` in AP subtraction methods and regression test coverage. |
| BLK-007 | HIGH | Quest Consequences | reputation/global-var branch continuity | consequence gate evaluation + persistence | No dedicated regression suite verified that cross-town consequence gates (karma/reputation + GVAR conditions) stay consistent after save/load. | Apply consequence actions (quest globals + reputation changes), save, load, re-evaluate branch gates. | Consequence-driven branch gates remain stable and deterministic across save/load. | `src/phase31.test.ts` | @engine | CLOSED | Added consequence harness covering NCR/Vault City/Broken Hills gate unlock/revoke + persistence. |
| BLK-008 | HIGH | Cinematics | slide timing robustness | `CinematicPlayer._showSlide` duration handling | Invalid slide durations (negative/NaN) could produce undefined playback timing behavior in ending/intro sequences. | Play cinematic sequence with malformed slide duration values. | Invalid durations fall back to deterministic default timing; valid durations still honored. | `src/phase32.test.ts`, `src/phase3.test.ts` | @engine | CLOSED | Added duration validation fallback to default 4000 ms plus regression tests. |
| BLK-009 | HIGH | Certification | artifact consistency | critical-path docs/checkpoints consistency | No automated check ensured region checklist, blocker matrix, and phase checkpoint artifacts stayed synchronized and structurally complete. | Run consistency assertions against docs for required regions, blocker states, and phase 0-8 checkpoint completeness. | Certification artifacts stay in sync and fail loudly in CI when structure drifts. | `src/phase33.test.ts` | @engine | CLOSED | Added certification artifact integrity suite for region list + blocker/checkpoint consistency. |
| BLK-010 | HIGH | Combat | called-shot robustness | `Combat.getHitChance` region handling | Unknown called-shot region keys could produce NaN hit calculations and throw, risking runtime combat interruption in scripts/modded inputs. | Call hit/roll with an unrecognized region string. | Unknown regions safely fall back to torso modifiers without throwing. | `src/phase30.test.ts`, `src/combat.integration.test.ts` | @engine | CLOSED | Added torso-fallback handling in hit-chance computation + regression coverage. |
| BLK-011 | HIGH | Region Certification | mid/late campaign continuity | mid/late route progression scaffold | No focused regression covered Modoc→Oil Rig critical-route gating and persistence as a single progression chain. | Execute staged progression through mid/late region gates and roundtrip save/load before final Oil Rig unlock. | Mid/late route globals, map-area discoveries, and final-route unlock remain stable and deterministic. | `src/phase34.test.ts` | @engine | CLOSED | Added mid/late campaign scaffold harness with gating + persistence assertions. |
| BLK-012 | HIGH | Release Gate | end-to-end campaign scaffold validation | full-playthrough harness | No single regression proved the full critical route could traverse early + mid/late scaffolds through Oil Rig ending trigger with save/load continuity. | Execute full route scaffold, save/load before and after ending trigger, verify Oil Rig endpoint and ending flag persistence. | End-to-end scaffold run reaches ending flow and persists ending trigger state across save/load. | `src/phase35.test.ts` | @engine | CLOSED | Added full critical-path scaffold run and release-gate sanity checks. |

| BLK-013 | HIGH | Save/Load | sfall global variable persistence | `sfallGlobals.ts` / `saveload.ts` | sfall string-keyed and integer-indexed global variables (`set_sfall_global` / `get_sfall_global` / `set_sfall_global_int` / `get_sfall_global_int`) were not persisted in saves, causing mods and scripts that use sfall globals for cross-map quest state to silently reset to zero on every save/load cycle. | Set sfall globals during gameplay, save, load — globals return to zero. | Save schema v11 adds `sfallGlobals: {stringKeyed, intIndexed}` field; saveload.ts serializes/deserializes both stores on every save/load; sanitization strips non-finite values. | `src/phase36.test.ts` | @engine | CLOSED | Added `serializeSfallGlobals`/`deserializeSfallGlobals`/`resetSfallGlobals` to sfallGlobals.ts; save schema v11 migration; save/load persistence. |

| BLK-014 | HIGH | Runtime/Scripting | crash-causing throw statements in scripting procedures | `src/scripting.ts` | Multiple scripting procedures used `throw` for edge-case inputs, crashing the browser runtime without recovery: `get_pc_stat` on unknown pcstat index; `mark_area_known` on area type > 1; `set_map_var` when no map script is attached; `critter_inven_obj` when a non-game-object is passed. | Call any of those procedures with out-of-range or invalid inputs (e.g. script passes null object to critter_inven_obj). | All four procedures return 0/null and emit a warning instead of throwing. No runtime crash. | `src/phase38.test.ts` | @engine | CLOSED | Changed all four throw paths to warn+safe-return; added regression tests for each. |

| BLK-015 | MEDIUM | Scripting/VM | metarule3 fallthrough bug and id<100 stub flood | `src/scripting.ts` | `metarule3(100, ...)` (CLR_FIXED_TIMED_EVENTS) fell through to `stub()` when the loop found no matching event, emitting a spurious stub hit and returning `undefined`. Additionally, metarule3 IDs below 100 had no safe fallback, routing to `stub()` and flooding the console. | Call `metarule3(100, obj, userdata, 0)` with a userdata value that matches no registered event; or call `metarule3(50, ...)`. | `metarule3(100, ...)` returns 0 always; metarule3 IDs < 100 return 0 silently. | `src/phase38.test.ts` | @engine | CLOSED | Added explicit `return 0` in id=100 loop; added id<100 silent default before all else-if chains; removed trailing `stub('metarule3', ...)` call. |

| BLK-017 | HIGH | Scripting/Dialogue | `getScriptMessage` throw paths | `src/scripting.ts` | `getScriptMessage` threw on missing message file or missing key, crashing any dialogue/message script that referenced an unloaded message. | Call `message_str(scriptID, msgNum)` where the message file fails to load or the message key is absent. | Missing file → warn + return null; missing key → warn + return empty string. No runtime crash. | `src/phase39.test.ts` | @engine | CLOSED | Converted both throw paths to warn+safe return (null / empty string). |

| BLK-018 | HIGH | Inventory/Economy | `item_caps_total` throw on non-game-object | `src/scripting.ts` | `item_caps_total(obj)` threw when `obj` was not a game object (deleted reference, null, etc.), crashing barter and economy scripts. | Call `item_caps_total(null)` or pass a non-object in barter flow. | Returns 0 with warning. No crash. | `src/phase39.test.ts` | @engine | CLOSED | Changed throw to warn + return 0. |

| BLK-019 | HIGH | Map/Objects | `create_object_sid` invalid elevation throw | `src/scripting.ts` | `create_object_sid(pid, tile, elev, sid)` threw on elevation out of [0,2], crashing map-init scripts that pass data-file elevation constants. | Call with `elev=-1` or `elev=3`. | Clamps elevation to [0,2] with warning, creates object normally. | `src/phase39.test.ts` | @engine | CLOSED | Changed throw to warn + clamp `elev = Math.max(0, Math.min(2, elev))`. |

| BLK-020 | HIGH | Dialogue | `start_gdialog`/`gdialog_mod_barter`/`gsay_reply` throw paths | `src/scripting.ts` | Three dialogue procedures threw when `self_obj` was missing or message was null: `start_gdialog`, `gdialog_mod_barter` (throw on no self_obj); `gsay_reply` (throw Error on null msg). | Trigger dialogue from a context without a self_obj, or call gsay_reply when message file is unavailable. | All three procedures warn and return/no-op instead of throwing. Dialogue simply skips without crashing. | `src/phase39.test.ts` | @engine | CLOSED | All three throw paths converted to warn+safe return/no-op. |

| BLK-021 | MEDIUM | World/Elevator | `metarule(15)` explicit type throw | `src/scripting.ts` | `metarule(15, target)` threw when `target !== -1`, crashing any script that called the elevator with an explicit type constant. | Call `metarule(15, 0)` (type 0 elevator). | Logs the explicit type and still invokes the elevator handler. No crash. | `src/phase39.test.ts` | @engine | CLOSED | Changed throw to log + proceed. |

| BLK-022 | MEDIUM | Scripting/Load | Message file parser throw on invalid line | `src/scripting.ts` | `loadMessageFile` threw on any line that didn't match the `{N}{}{...}` format, preventing any dialogue from loading when a message file had a comment or blank line. | Load a message file with a comment or malformed line. | Invalid lines are skipped with a warning; valid lines still load normally. | `src/phase39.test.ts` | @engine | CLOSED | Changed throw to warn + `continue`. |



---

| BLK-023 | HIGH | VM/Scripting | VM bridge stack safety | `src/vm_bridge.ts` | Any scripting procedure returning `undefined` (bare `return` in an early-return path) would push `undefined` onto the VM data stack. Subsequent arithmetic and comparisons on that `undefined` value would silently corrupt script execution, producing wrong quest flags and NaN-based stat calculations. `map_var()` (opcode 0x80C3) was the highest-frequency hit: both its "no map script" and "no script name" early-exit paths were bare `return` statements with no value. | Call `map_var()` from a critter script (no map script attached) or any bridged procedure that hits an early return without a value. | `push(r)` in `vm_bridge.ts` changed to `push(r ?? 0)` (systemic guard); `map_var()` early returns changed to `return 0` explicitly. No undefined value can reach the VM stack. | `src/phase44.test.ts` | @engine | CLOSED | push(r ?? 0) guard added in vm_bridge.ts; map_var bare returns fixed; regression tests for both. |

| BLK-024 | HIGH | Map/Objects | destroy_p_proc not fired | `src/map.ts` | `GameMap.destroyObject()` removed an object from the map but never invoked `Scripting.destroy(obj)`, so all `destroy_p_proc` callbacks were silently skipped. NPC death scripts (loot drops, quest state changes, dialog state) never fired on scripted object removal. | Script a critter with a `destroy_p_proc` and call `destroy_object()` on it; observe the destroy_p_proc callback is never executed. | `destroyObject()` now calls `Scripting.destroy(obj)` before `removeObject()`, with a reentrance guard (`_destroyingObjects: Set<Obj>`) to prevent infinite recursion if the script itself calls `destroy_object` on the same object. | `src/phase44.test.ts` | @engine | CLOSED | Scripting.destroy() called from destroyObject(); reentrance guard prevents self-destruct loops. |

| BLK-025 | MEDIUM | Combat | nextTurn infinite skip loop | `src/combat.ts` | `Combat.nextTurn()` recursively skipped dead/non-hostile combatants via `return this.nextTurn()`. With a large group of combatants where all AI slots cycle dead/non-hostile, the recursion depth could exhaust the call stack before `numActive === 0` terminates via `end()`. | Run a combat encounter where all AI combatants are killed mid-turn so that every remaining AI slot is dead when `nextTurn()` tries to assign their turn. | `nextTurn(skipDepth=0)` now accepts a skip counter incremented on each recursive dead/non-hostile skip. When `skipDepth > combatants.length + 2` the method forces `end()` instead of recursing, bounding the maximum stack depth to one full rotation of the combatant list. | `src/phase44.test.ts` | @engine | CLOSED | skipDepth guard forces end() after one full combatant-list rotation. |

- Phase 26 early-campaign route suite (`src/phase26.test.ts`) passed with no new critical blockers opened.
- UI2 interaction fidelity suites (`src/ui2/ui2.test.ts`, `src/ui2/panelParity.test.ts`) passed after barter exchange-commit fix.
- World-map reliability suites (`src/phase28.test.ts`, `src/worldmapEncounter.test.ts`) passed with saved-position normalization coverage.
- Save/load hardening suite (`src/saveload.test.ts`) passed with worldPosition + script-global roundtrip coverage.
- Combat fidelity suite (`src/combat.integration.test.ts`) passed after AP subtraction guard hardening.
- Quest consequence suite (`src/phase31.test.ts`) passed for karma/reputation/global-var gate persistence.
- Cinematic timing suite (`src/phase32.test.ts`) passed with malformed-duration guard coverage.
- Certification scaffolding suite (`src/phase33.test.ts`) passed for docs/checkpoint/blocker consistency.
- Phase 27/29/30 suites (`src/phase27.test.ts`, `src/phase29.test.ts`, `src/phase30.test.ts`) passed for interaction lifecycle, long-campaign save/load drift checks, and combat region fallback safety.
- Full project regression run (`npm test`) passed: 53 files / 1817 tests.
- Mid/late route scaffold suite (`src/phase34.test.ts`) passed for Modoc→Oil Rig gate/persistence continuity.
- Full-route scaffold suite (`src/phase35.test.ts`) passed for early→late→ending flow continuity.
- Phase 36 suite (`src/phase36.test.ts`) passed: sfall globals persistence (save schema v11), metarule default return safety, string_to_int/int_to_string opcodes, reg_anim_func ANIM_COMPLETE callbacks.
- Full project regression run (`npm test`) passed: 54 files / 1842 tests.
- Phase 38 suite (`src/phase38.test.ts`) passed: crash-causing throws hardened to safe returns (get_pc_stat, mark_area_known, set_map_var, critter_inven_obj); metarule3 id=100 fallthrough fixed; metarule3 id<100 silent default; proto_data default silenced; new sfall opcodes 0x8194–0x8197 (get_tile_fid, set_tile_fid, get_critter_flags, set_critter_flags).
- Full project regression run (`npm test`) passed: 56 files / 1906 tests.
- Phase 39 suite (`src/phase39.test.ts`) passed: dialogue/inventory/map/elevator crash-causing throws converted to safe returns (getScriptMessage, item_caps_total, create_object_sid, start_gdialog, gdialog_mod_barter, gsay_reply, metarule(15), message parser); anim negative-code stub silenced; new sfall opcodes 0x8198–0x819B (get_ini_setting, active_hand, set_sfall_return, get_sfall_arg).
- Full project regression run (`npm test`) passed: 57 files / 1953 tests.
- Phase 44 suite (`src/phase44.test.ts`) passed: VM bridge push(r ?? 0) undefined-stack guard (BLK-023); map_var() bare returns → return 0 (BLK-023); destroyObject() calls destroy_p_proc with reentrance guard (BLK-024); nextTurn() skip-depth guard prevents infinite recursion (BLK-025); new sfall opcodes 0x819C–0x81A2 (get_world_map_x/y, set_world_map_pos, in_world_map, get/set_critter_level, get_object_weight).
- Full project regression run (`npm test`) passed: 62 files / 2075 tests.
- Phase 46 suite (`src/phase46.test.ts`) passed: BLK-026 lookupScriptName warn+null; BLK-027 loadMessage skip-invalid-line; BLK-028 party.removePartyMember warn+no-op; BLK-029 saveSchema unknown-version warn+best-effort; BLK-030 char.ts SkillSet/StatSet getBase/get warn+0; BLK-031 critter_state full bitmask (knockedOut/crippled/isFleeing); BLK-032 obj_is_locked non-object default 1→0; validateSaveForHydration returns string|null.
- Full project regression run (`npm test`) passed: 64 files / 2151 tests.

| BLK-033 | HIGH | Save/Load | critter critical-injury flags not persisted | `src/object.ts` `SERIALIZED_CRITTER_PROPS` | Critter critical-injury and status flags (knockedOut, knockedDown, stunned, crippledLeftLeg, crippledRightLeg, crippledLeftArm, crippledRightArm, blinded, onFire, isFleeing) were not included in `SERIALIZED_CRITTER_PROPS`, so any crippled-limb or fleeing state was silently reset every time a save was loaded. | Save during or after combat with a crippled critter; reload; observe that the crippled state is gone. | All ten status-flag properties are now in `SERIALIZED_CRITTER_PROPS` and declared in `SerializedCritter`; the existing loop serializes/deserializes them automatically. | `src/phase50.test.ts` | @engine | CLOSED | Added to SERIALIZED_CRITTER_PROPS in object.ts; SerializedCritter interface updated with optional flag fields. |

| BLK-034 | MEDIUM | Save/Load | active_hand() always returned 0; active hand not persisted | `src/player.ts`, `src/scripting.ts`, `src/saveSchema.ts`, `src/saveload.ts` | The sfall `active_hand()` opcode always returned 0 (primary hand) regardless of which weapon slot the player had selected. The active-hand state was also not persisted in saves, so switching to the secondary weapon and saving/loading would always reset to primary. | Select secondary weapon slot, save, reload. `active_hand()` still returns 0. | `Player.activeHand` (number) added to Player class; `active_hand()` now reads the live value; save schema bumped to v13 with `playerActiveHand` field; both IDB and memory load paths restore the value. | `src/phase50.test.ts` | @engine | CLOSED | Player.activeHand tracks live state; save schema v13 persists it; defaults to 0 (primary) for old saves. |
| BLK-035 | HIGH | Save/Load | player base stats (HP, SPECIAL, skills) not persisted | `src/saveSchema.ts`, `src/saveload.ts` | Player base stats (HP, all SPECIAL attributes, radiation, poison) and skill values (base skills + unspent skill points) were not included in the save schema, so any stat modification by scripts or damage taken was silently reset to hardcoded defaults on every reload. | Receive damage (lower HP), save, load — HP resets to 100. | Save schema v14 adds `playerBaseStats`, `playerSkillValues`, `playerSkillPoints`; both IDB and memory load paths restore the full StatSet.baseStats and SkillSet.baseSkills + skillPoints on load. | `src/phase51.test.ts` | @engine | CLOSED | Added save schema v14 fields; both load paths restore player stats; migration defaults to {} for old saves. |
| BLK-036 | HIGH | Scripting/Map | `metarule3(106)` ignores elevation parameter on multi-floor maps | `src/scripting.ts` | METARULE3_TILE_GET_NEXT_CRITTER called `gMap.objectsAtPosition()` which always searches the current floor, ignoring the `elevation` argument. On Vaults, Oil Rig, and other multi-floor maps this returned wrong (or no) critters when the queried tile was on a different floor, causing patrol scripts and encounter triggers to silently fail. | Call metarule3(106, tile, 2, 0) from floor 0 on a multi-floor map with critters on floor 2. | Now uses `gMap.getObjects(elevation)` + position filter so the correct floor's critter list is searched. Also implements the `lastCritter` iteration parameter for enumerating multiple critters at a tile. | `src/phase52.test.ts` | @engine | CLOSED | getObjects(elevation) replaces objectsAtPosition; lastCritter iteration implemented. |
| BLK-037 | MEDIUM | Scripting/Map | `tile_contains_obj_pid` returns 0 for any non-current elevation | `src/scripting.ts` | `tile_contains_obj_pid(tile, elevation, pid)` returned 0 immediately whenever `elevation !== currentElevation`, even if the requested object existed on that floor. Multi-floor maps (Vaults) could not resolve object-presence checks across floors, causing locked-door and trigger scripts to malfunction. | Call `tile_contains_obj_pid(tile, 2, pid)` from floor 0 when a valid object with that PID is on floor 2. | Now uses `gMap.getObjects(elevation)` with position + PID filtering, identical to the `tile_contains_pid_obj` procedure. | `src/phase52.test.ts` | @engine | CLOSED | getObjects(elevation) + position/PID filter replaces cross-elevation early-return guard. |
| BLK-038 | MEDIUM | Scripting/VM | sfall opcodes 0x81BE–0x81C5 caused unknown-opcode halts | `src/vm_bridge.ts` | Eight sfall extended opcodes (get_critter_weapon, critter_inven_size, get_sfall_args_count, get_sfall_arg_at, set_sfall_arg, get_object_lighting, get_critter_team, set_critter_team) were absent from the opcode table. Scripts compiled against sfall 4.x that call any of these would hit an unknown-opcode warning and corrupt the VM stack (unpushed return values), producing wrong quest flags and silent NPC behaviour failures. | Call any of these opcodes from a Fallout 2 sfall-compiled script. | All eight opcodes added: get_critter_weapon and get/set_critter_team are fully implemented; others are safe partial stubs that correctly pop arguments and push return values. | `src/phase52.test.ts` | @engine | CLOSED | Eight opcodes added in vm_bridge.ts and scripting.ts; checklist updated. |
| BLK-039 | HIGH | Save/Load | NPC weapon slots (leftHand/rightHand) not persisted | `src/object.ts` | NPC critter `leftHand` and `rightHand` weapon objects were not included in the serialized map object, so any weapon equipped by an NPC via `wield_obj_critter` was silently lost on map reload. NPCs would always start unarmed (only default punch), breaking the combat AI of any script-equipped weapon bearer. | Equip an NPC with a weapon via `wield_obj_critter`, save, reload — NPC has no weapon. | `SERIALIZED_CRITTER_PROPS` extended with `leftHandPID`/`rightHandPID`; `Critter.toMapObject` serializes the PIDs; `objFromMapObject` deserializes by finding matching inventory items. | `src/phase53.test.ts` | @engine | CLOSED | BLK-039 in object.ts; weapon slot save/restore added with inventory-search on load. |
| BLK-040 | MEDIUM | Combat | AI attack crashes on dead target / critters without AP | `src/combat.ts` | `doAITurn()` could attempt to attack a target that had just died earlier in the same combat round, causing a null-dereference or animation crash. Also, critters added mid-round had no AP budget, causing divide-by-zero-like loops. | Kill a critter during combat; on the next AI turn the dead critter is targeted. | Added `if (target.dead) return` guard in `doAITurn` and an AP initialization check in `nextTurn`. | `src/phase53.test.ts` | @engine | CLOSED | BLK-040 guards in combat.ts; checklist updated. |
| BLK-041 | CRITICAL | Combat/XP | Player never gains XP from killing enemies | `src/critter.ts` | `critterKill()` did not auto-award XP to the player from the critter's proto `XPValue` field. In Fallout 2 the engine automatically awards base combat-kill XP; without this the player could never level up through combat, making skill progression impossible. | Kill any enemy in combat; observe player.xp never increases. | `critterKill()` now checks `source.isPlayer === true` and adds `pro.extra.XPValue` to `player.xp`, followed by an immediate level-up+skill-point check. | `src/phase54.test.ts` | @engine | CLOSED | Auto XP award added to critterKill; level-up and skill-point check mirrors Fallout 2 engine behaviour. |
| BLK-042 | HIGH | Save/Load | Player equipped weapon lost on save/load | `src/saveSchema.ts`, `src/saveload.ts` | Player-equipped weapons (leftHand/rightHand slots) were removed from `player.inventory` when drag-equipped in the UI but never separately serialized, so they were permanently lost across save/load cycles. The player would reload without their primary weapon. | Equip a weapon from inventory to leftHand, save, reload — the weapon is gone. | Save schema v15 adds `playerLeftHandPID`/`playerRightHandPID`; the save path serializes the equipped weapons and includes them in the inventory save; the load path re-equips them by PID lookup. | `src/phase54.test.ts` | @engine | CLOSED | BLK-042: save schema v15; both IDB and memory load paths restore weapon slots; migration defaults to undefined for old saves. |
| BLK-043 | HIGH | Progression | Skill points not awarded on level-up | `src/scripting.ts` | `give_exp_points()` leveled up the player but never awarded skill points, making it impossible to invest in skills through normal levelling. | Gain enough XP to level up; observe `player.skills.skillPoints` unchanged. | `give_exp_points()` now awards `max(1, 10 + floor(INT/2))` skill points per level gained, plus 2 per rank of the Educated perk (perk ID 47), matching Fallout 2 formula. | `src/phase54.test.ts` | @engine | CLOSED | BLK-043: skill points formula added to give_exp_points; Educated perk bonus applied. |
| BLK-044 | MEDIUM | Scripting | `inven_unwield` always cleared rightHand regardless of active hand | `src/scripting.ts` | `inven_unwield(obj)` always cleared `critter.rightHand` even for the player who uses `leftHand` as primary. Scripts calling `inven_unwield` expecting the active weapon to be removed would see no effect when the player's weapon was in leftHand. | Call `inven_unwield(player)` with player's weapon in leftHand (activeHand=0); weapon stays in leftHand. | For the player, `inven_unwield` now checks `activeHand` and clears `leftHand` when `activeHand=0`, `rightHand` when `activeHand=1`. NPC behaviour unchanged. | `src/phase54.test.ts` | @engine | CLOSED | BLK-044: activeHand-aware inven_unwield for player; NPC still uses rightHand. |

- Phase 50 suite (`src/phase50.test.ts`) passed: BLK-033 critter status flags serialized; BLK-034 active hand tracked and persisted; save schema v13; sfall opcodes 0x81AE–0x81B5; checklist upgrades (proto_data, tile_is_visible, metarule_18/21/35/44/46/55, anim, proto_data_flags2).
- Full project regression run (`npm test`) passed: 68 files / 2336 tests.
- Phase 51 suite (`src/phase51.test.ts`) passed: BLK-035 player stats/skills persisted in save schema v14; sfall opcodes 0x81B6–0x81BD; checklist upgrades.
- Full project regression run (`npm test`) passed: 69 files / 2383 tests.
- Phase 52 suite (`src/phase52.test.ts`) passed: BLK-036 metarule3(106) elevation-aware; BLK-037 tile_contains_obj_pid elevation-aware; BLK-038 sfall opcodes 0x81BE–0x81C5; get_critter_weapon/team; checklist upgrades.
- Full project regression run (`npm test`) passed: 70 files / 2447 tests.
- Phase 53 suite (`src/phase53.test.ts`) passed: BLK-039 NPC weapon slot save/restore; BLK-040 AI dead-target guard + AP init check; sfall opcodes 0x81C6–0x81CF; checklist upgrades.
- Full project regression run (`npm test`) passed: 71 files / 2519 tests.
| BLK-045 | HIGH | Save/Load | Player equipped armor lost on save/load | `src/saveSchema.ts`, `src/saveload.ts` | Player-equipped armor (`equippedArmor`) was not serialized when saving. Armor dragged to the equipment slot is removed from inventory and stored only in `player.equippedArmor`, so it would be lost across save/load cycles — the player would reload without their Combat Armor, Leather Armor, etc. | Equip armor from inventory, save, reload — armor is gone, player reverts to default clothing. | Save schema v16 adds `playerArmorPID`; the save path serializes the armor into inventory and records the PID; the load path re-equips it by PID lookup. | `src/phase55.test.ts` | @engine | CLOSED | BLK-045: save schema v16; both IDB and memory load paths restore equippedArmor; migration defaults to undefined for old saves. |
| BLK-046 | MEDIUM | Save/Load | Missing `party` field in old saves causes aborted load | `src/saveSchema.ts` | `validateSaveForHydration` requires `Array.isArray(save.party)` but old saves without a `party` field would fail this check and abort the entire load, leaving the game in an unchanged state and silently losing user progress. | Load any save object that predates the `party` feature (no party field present). | `migrateSave()` normalization pass now sets `save.party = []` when the field is absent or non-array. Load never aborts due to a missing party. | `src/phase55.test.ts` | @engine | CLOSED | BLK-046: defensive `Array.isArray(save.party) ? save.party : []` normalization added to migrateSave. |
| BLK-047 | MEDIUM | Progression | Perk credits not tracked on level-up | `src/scripting.ts`, `src/globalState.ts`, `src/saveSchema.ts` | `give_exp_points()` leveled up the player and awarded skill points but never tracked perk-selection credits. Fallout 2 grants one perk every 3 levels; `get_perk_owed()` always returned 0 and `set_perk_owed()` was a no-op, so mods and scripts checking for available perks would always see zero. | Level the player to 3/6/9 via `give_exp_points()`; call `get_perk_owed()` — returns 0 despite a perk being due. | `give_exp_points()` increments `globalState.playerPerksOwed` by 1 whenever `player.level % 3 === 0`. `get_perk_owed()` (sfall 0x81AE) returns the actual counter; `set_perk_owed()` (0x81AF) writes it. Persisted in save schema v16. | `src/phase55.test.ts` | @engine | CLOSED | BLK-047: playerPerksOwed counter added; give_exp_points awards perk credits; get/set_perk_owed functional; save schema v16. |

| BLK-048 | HIGH | Save/Load | Player name and gender not persisted | `src/saveSchema.ts`, `src/saveload.ts` | `player.name` (set during character creation and by scripts via `set_name`) and `player.gender` (checked by many dialogue scripts via `get_critter_stat(dude_obj, STAT_gender)`) were not included in the save schema. On every reload, both reverted to class defaults ("Player" / "male"), breaking gender-gated dialogue and displaying the wrong character name. | Set a custom character name, save, reload — name reverts to "Player". | Save schema v17 adds `playerName` and `playerGender` fields; `save()` snapshots them; `applyExtraSaveState()` restores them; migration from v16 defaults both to their class defaults. | `src/phase56.test.ts` | @engine | CLOSED | BLK-048: save schema v17; both fields snapshotted and restored; migration defaults added; normalization rejects invalid values. |
| BLK-049 | MEDIUM | Progression | Level-up via critterKill() missing Educated perk bonus and perk credits | `src/critter.ts` | The XP-from-kill level-up loop in `critterKill()` (added in BLK-041) awarded skill points using only the base formula (10 + INT/2) and never incremented `playerPerksOwed`. This diverged from `give_exp_points()` (which correctly applies Educated perk +2/rank and awards perk credits every 3 levels), causing inconsistent progression depending on whether XP came from a kill or a script call. | Kill enemies until level 3; observe no perk credit is awarded via critterKill path. | `critterKill()` now applies Educated perk bonus and increments `playerPerksOwed` every 3 levels, mirroring `give_exp_points()`. | `src/phase56.test.ts` | @engine | CLOSED | BLK-049: Educated perk bonus and perk-credit logic added to critterKill() level-up loop. |
| BLK-050 | MEDIUM | Scripting/VM | `set_name(obj, name)` opcode (0x80A8) absent | `src/scripting.ts`, `src/vm_bridge.ts` | The standard Fallout 2 opcode `set_name(obj, name)` (0x80A8) was not registered in the opcode dispatch table. Any script calling it (including character-creation scripts that set the player's name and NPC scripts that rename critters) would leave an extra argument on the VM stack, silently corrupting all subsequent opcode argument reads for that script. | Run any script containing `set_name(dude_obj, "Name")`. | Opcode is registered and dispatched to `Script.set_name(obj, name)`, which assigns `name` to `obj.name`. | `src/phase56.test.ts` | @engine | CLOSED | BLK-050: `set_name` implemented in scripting.ts; opcode 0x80A8 registered in vm_bridge.ts. |

- Phase 56 suite (`src/phase56.test.ts`) passed: BLK-048 player name/gender save (schema v17); BLK-049 critterKill level-up consistency; BLK-050 set_name opcode; sfall opcodes 0x81E0–0x81E7; checklist upgrades.
- Full project regression run (`npm test`) passed: 74 files / 2617 tests.

- Phase 73 suite (`src/phase73.test.ts`) passed: BLK-100 play_sfx null-audioEngine guard; BLK-101 walkTo null-position guard; BLK-102 walkTo window.performance.now() guard; BLK-103 map loadMap null-audioEngine guard; BLK-104 reg_anim_obj_move_to_tile null-position guard; sfall opcodes 0x8268–0x826F.
- Full project regression run (`npm test`) passed: 91 files / 3212 tests.

| BLK-076 | HIGH | Scripting/Map | null-gMap crash in objCanSeeObj | `src/scripting.ts` | `objCanSeeObj()` called `globalState.gMap.hexLinecast()` unconditionally.  During map transitions or before the first map loads, gMap is null and the call threw a TypeError, crashing any script calling `can_see_obj` (e.g. combat AI perception checks, patrol-path decisions). | Call `obj_can_see_obj` from a script during a map transition (gMap=null). | Now guards: if `!globalState.gMap \|\| !obj.position \|\| !target.position`, returns `true` (conservatively unobstructed) instead of crashing. | `src/phase68.test.ts` | @engine | CLOSED | Added null guard before hexLinecast call. |
| BLK-077 | HIGH | Scripting/Gameplay | null-gMap crash in explosion() | `src/scripting.ts` | `explosion()` called `globalState.gMap.addObject()` and `removeObject()` without null check. Any script-triggered explosion during a map transition threw a TypeError. | Trigger explosion() when gMap is null. | Now emits a warning and returns early when gMap is null. | `src/phase68.test.ts` | @engine | CLOSED | Added null guard before addObject/removeObject. |
| BLK-078 | HIGH | Scripting/Map | null-gMap crash in load_map() | `src/scripting.ts` | `load_map()` called `globalState.gMap.loadMap()` / `loadMapByID()` without checking gMap is non-null. Scripts that call `load_map` before a map is initialized threw a TypeError. | Call load_map() before map init. | Now emits a warning and returns early when gMap is null. | `src/phase68.test.ts` | @engine | CLOSED | Added null guard before map load call. |
| BLK-079 | MEDIUM | Scripting/Map | null-gMap crash in create_object_sid() | `src/scripting.ts` | `create_object_sid()` called `globalState.gMap.addObject()` without null check. Threw TypeError when called before map init. | Call create_object_sid() before map load. | Now emits a warning and returns null when gMap is null. | `src/phase68.test.ts` | @engine | CLOSED | Added null guard before addObject call. |
| BLK-080 | MEDIUM | Save/Load | null-gMap crash in save() log message | `src/saveload.ts` | `save()` accessed `globalState.gMap.name` in a console.log without null check. Called before map init threw TypeError. | Call save() before a map is loaded. | Now uses optional chaining: `gMap?.name ?? "(none)"`. | `src/phase68.test.ts` | @engine | CLOSED | Optional chaining applied. |
| BLK-081 | HIGH | Objects/Scripting | null proto crash in Obj.fromPID_() | `src/object.ts` | `Obj.fromPID_()` accessed `obj.pro.flags` unconditionally after `loadPRO()`. When no .pro file exists (unknown PID, test environments, corrupted data), `loadPRO` returns null and the access threw a TypeError. This crashed `createObjectWithPID`, `create_object_sid`, `explosion`, and NPC item equip. | Call `createObjectWithPID` with a PID that has no .pro file. | `obj.flags = pro != null ? pro.flags : 0`; pro-dependent init skipped when pro is null. | `src/phase68.test.ts` | @engine | CLOSED | null guard in Obj.fromPID_; object usable with zero flags. |
| BLK-082 | MEDIUM | Scripting/UI | `window.performance.now()` crash in `float_msg()` | `src/scripting.ts` | `float_msg()` called `window.performance.now()` directly. In Node.js test environments (and some non-browser runtimes) the `window` global does not exist, so every float message call threw a ReferenceError, crashing map-enter scripts that display player feedback. | Any script calling `float_msg()` during a map-enter or combat event in a non-browser context. | Now uses `typeof performance !== 'undefined' ? performance.now() : 0`, matching the safe-fallback pattern used by `get_uptime()`. | `src/phase69.test.ts` | @engine | CLOSED | Safe `performance.now()` with typeof guard applied. |
| BLK-083 | MEDIUM | Scripting/Map | null `player.position` crash in `tile_is_visible()` | `src/scripting.ts` | `tile_is_visible()` checked `globalState.player` before calling `hexDistance(globalState.player.position, tilePos)`, but did not guard against `player.position` being null. During early script execution (before `map_enter_p_proc` completes), the player object exists but has not yet been placed on the map. `hexDistance(null, …)` threw a TypeError. | Call any script that invokes `tile_is_visible` during map-enter before the player is positioned. | Now returns 1 (always visible) when `player.position` is null. | `src/phase69.test.ts` | @engine | CLOSED | Null guard added before `hexDistance` call. |
| BLK-084 | MEDIUM | Scripting/Map | non-null assertion crash in `set_exit_grids()` | `src/scripting.ts` | `set_exit_grids()` used `gameObjects!` (TypeScript non-null assertion) without a real null check. Scripts that call `set_exit_grids` during startup or before `enterMap`/`updateMap` are invoked (when `gameObjects` is null) received a TypeError. | Call `set_exit_grids` from a map-init script before `enterMap` has set the object list. | Now guards with an explicit null check and early warning return when `gameObjects` is null. | `src/phase69.test.ts` | @engine | CLOSED | Null guard replaces non-null assertion. |
| BLK-085 | MEDIUM | Scripting/Combat | null position crash in `obj_can_hear_obj()` | `src/scripting.ts` | `obj_can_hear_obj()` called `hexDistance(a.position, b.position)` without checking that either position is non-null. Objects in inventory or mid-map-transition often have a null position; `hexDistance(null, …)` threw a TypeError, crashing any combat-AI hearing check. | Use `obj_can_hear_obj` with a critter that has been removed from the map (position=null). | Now returns 0 (out of earshot) when either position is null. | `src/phase69.test.ts` | @engine | CLOSED | Position null guard added before hexDistance call. |
| BLK-086 | HIGH | Scripting/Combat | null position crash in `canSee()` | `src/scripting.ts` | `canSee()` called `hexDirectionTo(obj.position, target.position)` without null-checking either position. Objects in inventory or mid-map-transition have null positions; `hexDirectionTo(null, …)` threw a TypeError, crashing any LOS/perception check that calls `canSee`. | Call `obj_can_see_obj` with a critter whose position is null. | Now returns `false` (cannot see) when either position is null. | `src/phase70.test.ts` | @engine | CLOSED | Position null guard added before hexDirectionTo call. |
| BLK-087 | HIGH | Scripting/Combat | null position crash in `isWithinPerception()` | `src/scripting.ts` | `isWithinPerception()` called `hexDistance(obj.position, target.position)` without null-checking either position. Critters in inventory or mid-transition have null positions; `hexDistance(null, …)` threw a TypeError, crashing any NPC perception check. | Run an encounter where one critter has a null position. | Now returns `false` (not within perception) when either position is null. | `src/phase70.test.ts` | @engine | CLOSED | Position null guard added before hexDistance call. |
| BLK-088 | MEDIUM | Scripting/Map | null position crash in metarule3(106) filter | `src/scripting.ts` | metarule3(106) (`tile_get_next_critter`) filtered all objects and accessed `o.position.x` without first verifying that `o.position` was non-null. Any critter object with a null position in the object list (inventory, mid-transition) caused a TypeError crash. | Have a critter without a position in the map's object list, then call `tile_get_next_critter`. | Filter now guards `o.position !== null` before accessing x/y. | `src/phase70.test.ts` | @engine | CLOSED | Null guard added to filter callback. |
| BLK-089 | MEDIUM | Scripting/Combat | null position crash in `num_critters_in_radius()` | `src/scripting.ts` | `num_critters_in_radius()` called `hexDistance(origin, obj.position)` without checking that `obj.position` is non-null. Critters in inventory or mid-transition have null positions, causing hexDistance to crash. | Call `num_critters_in_radius` when map contains critters with null positions. | Now skips objects with null positions instead of crashing. | `src/phase70.test.ts` | @engine | CLOSED | Position null guard added before hexDistance call. |
| BLK-090 | HIGH | Scripting/Critter | null position crash in `updateCritter()` | `src/scripting.ts` | `updateCritter()` called `toTileNum(obj.position)` without null-checking `obj.position`. Critters not yet placed on a map (or whose position was cleared during a map transition) caused `toTileNum(null)` to crash with a TypeError, preventing critter_p_proc from running. | Load a map with a critter that has no position set. | Now uses `obj.position ? toTileNum(obj.position) : 0` as a safe fallback tile. | `src/phase70.test.ts` | @engine | CLOSED | Null guard with fallback tile 0 added. |
| BLK-100 | HIGH | Scripting/Audio | null audioEngine crash in `play_sfx()` | `src/scripting.ts` | `play_sfx()` called `globalState.audioEngine.playSfx(sfx)` without null check. `globalState.audioEngine` is null during test environments and before the browser audio subsystem is initialized. Any script that calls `play_sfx()` (e.g. map-enter ambient sounds, combat cues) threw a TypeError, crashing the entire script. | Call `play_sfx("sfx_test")` when `globalState.audioEngine` is null (test env or early init). | Now guards with `if (!globalState.audioEngine) return` before the call. | `src/phase73.test.ts` | @engine | CLOSED | Null guard added; audio call skipped silently when engine not initialized. |
| BLK-101 | HIGH | Objects/Movement | null position crash in `walkTo()` | `src/object.ts` | `walkTo()` accessed `this.position.x` unconditionally at the very start of the function. Critters in inventory or cleared mid-map-transition have `position=null`. Any movement command issued to an unplaced critter (via AI, script, or combat) crashed with a TypeError. | Issue a `walkTo` call on a critter whose position has been cleared. | Now returns `false` immediately when `this.position` is null. | `src/phase73.test.ts` | @engine | CLOSED | Null position guard added as first check in walkTo(). |
| BLK-102 | MEDIUM | Objects/Movement | `window.performance.now()` crash in `walkTo()` | `src/object.ts` | `walkTo()` used `window.performance.now()` directly. In Node.js test environments and non-browser runtimes `window` is not defined, so any animation started via `walkTo` threw a ReferenceError. | Start a walk animation in a non-browser context. | Now uses `typeof performance !== 'undefined' ? performance.now() : 0`, the same safe-fallback pattern as `float_msg()` (BLK-082). | `src/phase73.test.ts` | @engine | CLOSED | Safe `performance.now()` with typeof guard applied. |
| BLK-103 | HIGH | Map/Audio | null audioEngine crash in `map.ts` loadMap | `src/map.ts` | `loadMap()` called `globalState.audioEngine.stopAll()` and `audioEngine.playMusic()` unconditionally. `globalState.audioEngine` is null in test environments and before the audio subsystem is created. The crash aborted map loading entirely when called outside the browser. | Load a map in any test environment (audioEngine is null). | Now guarded: both audio calls are wrapped in `if (globalState.audioEngine)`. | `src/phase73.test.ts` | @engine | CLOSED | Null guard wraps both stopAll() and playMusic() calls. |
| BLK-104 | MEDIUM | Scripting/Movement | null position crash in `reg_anim_obj_move_to_tile()` | `src/scripting.ts` | `reg_anim_obj_move_to_tile()` checked that the critter had a `walkTo` method but did not guard against a null position before calling it. `walkTo()` accesses `this.position.x` immediately, so any critter without a map position crashed with a TypeError. | Call `reg_anim_obj_move_to_tile` on a critter that has no position set. | Now checks `if (!obj.position)` before calling `walkTo` and returns with a warning. | `src/phase73.test.ts` | @engine | CLOSED | Position null guard added before walkTo call. |

## Closure checklist (required)

Before moving a blocker to `CLOSED`, confirm:

- [ ] Root cause identified in code and linked in Notes
- [ ] Fix merged in source with deterministic behavior
- [ ] Regression test added/updated and passing
- [ ] Region checklist status updated in `docs/F2_CRITICAL_PATH.md`
- [ ] Any related VM checklist status updated (when applicable)

| BLK-105 | HIGH | Scripting/Time | `src/scripting.ts` | `game_time_advance` | Non-finite ticks (NaN/Infinity) corrupted `globalState.gameTickTime`, breaking all time-based events. | Pass NaN or Infinity as ticks argument to `game_time_advance()`. | Guard with `isFinite()` and return early. No time corruption. | `src/phase74.test.ts` | @engine | CLOSED | Added `isFinite()` guard; logs warning and returns early. |

| BLK-106 | HIGH | Scripting/XP | `src/scripting.ts` | `give_exp_points` | Non-finite XP (NaN/Infinity) corrupted `player.xp`, permanently preventing level-up (NaN >= threshold is always false). | Pass NaN as xp to `give_exp_points()`. | Guard with `isFinite()` and return early. XP unchanged. | `src/phase74.test.ts` | @engine | CLOSED | Added `isFinite()` guard; logs warning and returns early. |

| BLK-107 | HIGH | Scripting/Dialogue | `src/scripting.ts` | `gsay_option` | Null/non-function target caused `target.bind(this)` to throw a TypeError, aborting the entire dialogue session. | Pass 0 or a non-callable as the target argument to `gsay_option()`. | Substitute a no-op handler; dialogue option still appears in UI. | `src/phase74.test.ts` | @engine | CLOSED | Added `typeof target !== 'function'` guard; substitutes no-op. |

| BLK-108 | HIGH | Scripting/Map | `src/scripting.ts` | `critter_attempt_placement` | Delegated to `move_to()` without null-checking gMap; `gMap.changeElevation()` threw TypeError during map transitions. | Call `critter_attempt_placement()` while `globalState.gMap` is null. | Return -1 (placement failure) when gMap is null. | `src/phase74.test.ts` | @engine | CLOSED | Added null gMap check before calling `move_to()`; returns -1. |

| BLK-109 | MEDIUM | Scripting/Timers | `src/scripting.ts` | `add_timer_event` | Zero or negative ticks caused events to fire on the very next `game_time_advance()` call, producing re-entrant callbacks. | Call `add_timer_event()` with ticks <= 0. | Clamp ticks to minimum of 1. | `src/phase74.test.ts` | @engine | CLOSED | Sets ticks = 1 when ticks is <= 0, non-finite, or non-number. |

| BLK-139 | HIGH | Global/Browser | `src/main.ts` | Global error handler | Unhandled JS exceptions (from WebGL errors, bad scripts reaching the game loop, or async failures) silently froze the game — no user feedback, no recovery option. | Any uncaught exception in the browser tab. | Global window.onerror and unhandledrejection handlers show a recoverable overlay (Continue / Reload) and log to console. Throttled to 5/minute. | `src/phase82.test.ts` (checklist) | @engine | CLOSED | installBrowserErrorBoundary() IIFE added to main.ts; installs both handlers. |

| BLK-140 | HIGH | Global/Scripting | `src/scripting.ts` | All 18 script trigger dispatch functions | Any uncaught exception thrown inside a script procedure (talk_p_proc, critter_p_proc, map_update_p_proc, etc.) propagated up uncaught through the game loop, crashing the session. | Execute any script procedure that throws an internal error. | callProcedureSafe() wraps every procedure dispatch; logs warning and returns safe default (false / null). Game loop continues. | `src/phase82.test.ts` | @engine | CLOSED | callProcedureSafe() helper added; applied to all 18 trigger dispatch points across talk, updateCritter, timedEvent, use, lookAt, description, useSkillOn, pickup, useObjOn, push, isDropping, combatEvent, updateMap (map+objects), exitMap, enterMap, objectEnterMap, spatial, destroy, damage. |

| BLK-142 | HIGH | Scripting/Map | `src/scripting.ts` | `updateMap` per-object loop | One NPC's map_update_p_proc throwing aborted the entire object update loop — all subsequent NPCs on the map stopped running their heartbeat scripts. | Execute updateMap() with one NPC whose map_update_p_proc throws. | Each NPC's map_update_p_proc is wrapped independently; throwing NPCs are skipped, others continue. | `src/phase82.test.ts` | @engine | CLOSED | Per-object callProcedureSafe() in the updateMap loop. |

| BLK-143 | MEDIUM | Scripting/Timers | `src/scripting.ts` | `timedEvent` | A throwing timed_event_p_proc corrupted the timer event chain, preventing subsequent timer callbacks from firing. | Execute timedEvent() on a script whose timed_event_p_proc throws. | Timer callback wrapped in callProcedureSafe(); subsequent timers fire normally. | `src/phase82.test.ts` | @engine | CLOSED | callProcedureSafe() applied to timed_event_p_proc dispatch in timedEvent(). |

| BLK-144 | HIGH | Scripting/Map-Load | `src/scripting.ts` | `initScript` start proc | A script whose `start` procedure throws (bad initializer, missing resource, null-ref in startup code) aborted map-load for all subsequent objects — every NPC after the bad one failed to initialize. | Load a map containing at least one object whose start proc throws. | `initScript()` wraps the `start` call in `callProcedureSafe()`; a throwing initializer is logged and skipped, all subsequent objects on the map still initialize. | `src/phase83.test.ts` | @engine | CLOSED | callProcedureSafe() applied to initScript() start proc dispatch. |
